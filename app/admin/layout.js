export default function AdminLayout({ children }) {
    return (
        <div>
            {/* This is where all admin page components will be rendered */}
            {children}
        </div>
    );
}
